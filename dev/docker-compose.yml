
services:
  code-server:
    build: .
    container_name: code-server-jekyll
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Oslo
      - PASSWORD=password
    volumes:
      - ~/.ssh:/config/.ssh
      - ~/.gitconfig:/config/.gitconfig
      - ../.:/config/rapporteket.github.io
    ports:
      - "8443:8443" # code-server web UI
      - "4000:4000" # Direct port mapping for Jekyll
